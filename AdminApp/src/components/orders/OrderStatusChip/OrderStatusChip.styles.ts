import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  chip: {
    alignSelf: 'flex-start',
  },
  chipText: {
    fontSize: 11,
    fontWeight: 'bold',
    marginVertical: 0,
  },
});
